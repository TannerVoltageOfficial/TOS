import { version as osVersion } from "./osVars"
// English (US) localization strings
export namespace en_us {
    export namespace menuItems {
        export var about = "About T-OS";
        export var logout = "Logout";
    }

    export var version = "Version: " + osVersion;
}
// Spanish localization strings
export namespace es {
    export namespace menuItems {
        export var about = "Acerca de T-OS";
        export var logout = "Cerrar sesión";
    }
    export var version = "Versión: " + osVersion;
}